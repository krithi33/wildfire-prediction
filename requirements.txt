# Core geospatial libraries
earthengine-api>=0.1.350  # Google Earth Engine Python API
geopandas>=0.14.0         # Geospatial dataframes (extends pandas with geometry)
rasterio>=1.3.8           # Reading/writing raster data (satellite images)
shapely>=2.0.0            # Geometric operations (points, polygons, etc.)
pyproj>=3.6.0             # Coordinate system transformations

# Data manipulation
pandas>=2.0.0             # Dataframes for tabular data
numpy>=1.24.0             # Numerical computing (arrays, linear algebra)
xarray>=2023.1.0          # Multi-dimensional arrays (time × lat × lon)

# Machine learning
scikit-learn>=1.3.0       # ML algorithms, preprocessing, validation
lightgbm>=4.0.0           # Gradient boosting (fast, accurate for tabular data)
imbalanced-learn>=0.11.0  # Tools for handling class imbalance

# Visualization
matplotlib>=3.7.0         # Basic plotting
seaborn>=0.12.0           # Statistical visualizations
folium>=0.14.0            # Interactive maps
plotly>=5.14.0            # Interactive plots

# Jupyter
jupyter>=1.0.0            # Notebook interface
ipykernel>=6.25.0         # Jupyter kernel for Python

# Optional: Deep learning (if we use ConvLSTM)
# torch>=2.0.0            # PyTorch for neural networks
# torchvision>=0.15.0     # Image transformations

# Optional: SHAP for model interpretability
# shap>=0.42.0            # Explain model predictions

# Utilities
tqdm>=4.65.0              # Progress bars
python-dotenv>=1.0.0      # Load environment variables from .env file
pyarrow
